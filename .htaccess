Options -Indexes

RewriteEngine On

# File hoặc thư mục có thật thì cho phép truy cập
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# URL không tồn tại -> index.php
RewriteRule ^ index.php [L]